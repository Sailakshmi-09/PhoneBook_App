main.c   : include headers,then in main take using switch case written functions
  ||
create.c : In this first creating name and number then later in while loop if we want to add multiple numbers,
  ||        there asks if you want to add (Y/N)choice if yes add another number,if no(N/n) goto next information like mail,addess.
print.c  : In this the creating before data should be printing. 
  ||
delete.c : In this function based on given name by the user which you want to delete that should be deleted all information about that name,else it should print name not found.
  ||
find.c   : In this function based on given name the details of that name should be display,else it prints file neme not found.
  ||
edit.c   : In this function based on given name by the user then if you want to change the name that should be changed selects on choice (y/n) 
  |         if selects yes that anme should be changed if no that should be ask for next information that if you want to add (y/n)
  |         option will show if yes add another information if no goto next information.
save.c   : In this function that data all created before should be stored and written into file.
  ||
sync_file.c: In this function the already written file should be reading if after executing before execution data also display,when again you execute.
  ||
makefile :  In this makefile all files above a compiled at a time ,if any modification is happen then that time only that file should be compiled,that all files compile using make command.
  ||
headers.h  : In this function all standard library files should be include what shoukd we used in that files.function declarations,structure also defined in headers.h.
 
